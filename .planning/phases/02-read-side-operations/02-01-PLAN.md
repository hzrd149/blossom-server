---
phase: 02-read-side-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/delete.ts
  - tests/e2e/delete.test.ts
autonomous: true
requirements:
  - DELT-01
  - DELT-02
must_haves:
  truths:
    - "DELETE /<sha256> returns 204 No Content on successful deletion when only ownership is removed"
    - "DELETE /<sha256> returns 204 No Content on successful deletion when blob is fully purged"
    - "DELETE /<sha256> returns 404 when the blob does not exist"
  artifacts:
    - path: "src/routes/delete.ts"
      provides: "DELETE handler with 204 status"
      contains: "ctx.body(null, 204)"
    - path: "tests/e2e/delete.test.ts"
      provides: "E2E tests for DELETE endpoint status codes"
      min_lines: 80
  key_links:
    - from: "src/routes/delete.ts"
      to: "src/middleware/errors.ts"
      via: "errorResponse(ctx, 404, ...)"
      pattern: "errorResponse.*404"
---

<objective>
Update DELETE /<sha256> to return 204 No Content on successful deletion (instead of 200) and add E2E tests covering all DELETE status codes.

Purpose: DELT-01 requires 204 for successful deletions. DELT-02 requires 404 for non-existent blobs. The code change is two lines; the tests prove the behavior.
Output: Updated delete.ts handler, new delete.test.ts E2E test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-read-side-operations/02-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/routes/delete.ts (current implementation):
```typescript
// Line 87: ctx.body(null, 200)  — ownership removed, other owners remain
// Line 99: ctx.body(null, 200)  — blob fully purged
// Both must change to ctx.body(null, 204) per D-01
```

From src/middleware/errors.ts:
```typescript
export function errorResponse(
  ctx: Context,
  status: 400 | 401 | 403 | 404 | 409 | 411 | 413 | 415 | 416 | 422 | 429 | 500 | 502 | 503 | 507,
  reason: string,
): Response;
```

From tests/e2e/upload.test.ts (test pattern to follow):
```typescript
// Setup: initDb, LocalStorage, initPool, buildApp, ConfigSchema.parse
// Auth helpers: generateSecretKey, finalizeEvent, encodeBase64Url
// Test options: { sanitizeOps: false, sanitizeResources: false }
// Shared state via setup/teardown test pattern
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change DELETE success status from 200 to 204</name>
  <files>src/routes/delete.ts</files>
  <read_first>src/routes/delete.ts, .planning/phases/02-read-side-operations/02-CONTEXT.md</read_first>
  <action>
In `src/routes/delete.ts`, change the two `ctx.body(null, 200)` calls to `ctx.body(null, 204)`:

1. Line 87 (ownership removed, other owners remain): change `return ctx.body(null, 200);` to `return ctx.body(null, 204);`
2. Line 99 (blob fully purged): change `return ctx.body(null, 200);` to `return ctx.body(null, 204);`

Per D-01: DELETE returns 204 No Content for all successful deletions. No other changes to this file.
  </action>
  <verify>
    <automated>deno check src/routes/delete.ts && grep -c "ctx.body(null, 204)" src/routes/delete.ts | grep -q "2"</automated>
  </verify>
  <acceptance_criteria>
- src/routes/delete.ts contains exactly 2 occurrences of `ctx.body(null, 204)`
- src/routes/delete.ts contains 0 occurrences of `ctx.body(null, 200)`
- `deno check src/routes/delete.ts` passes with no type errors
  </acceptance_criteria>
  <done>DELETE handler returns 204 for both success paths (ownership-only removal and full purge)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create E2E tests for DELETE endpoint status codes</name>
  <files>tests/e2e/delete.test.ts</files>
  <read_first>tests/e2e/upload.test.ts, src/routes/delete.ts, src/middleware/auth.ts</read_first>
  <behavior>
    - Test: DELETE with valid auth on owned blob returns 204 with empty body
    - Test: DELETE on non-existent hash returns 404
    - Test: DELETE with invalid hash format returns 400
    - Test: DELETE without auth when auth required returns 401
    - Test: DELETE on blob not owned by caller returns 403
  </behavior>
  <action>
Create `tests/e2e/delete.test.ts` following the exact pattern from `tests/e2e/upload.test.ts`:

1. Import the same helpers: `assertEquals` from `@std/assert`, `encodeBase64Url` from `@std/encoding/base64url`, `encodeHex` from `@std/encoding/hex`, `crypto as stdCrypto` from `@std/crypto`, `join` from `@std/path`, `finalizeEvent`, `generateSecretKey`, `getPublicKey` from `nostr-tools/pure`, `initDb` from `../../src/db/client.ts`, `LocalStorage` from `../../src/storage/local.ts`, `initPool` from `../../src/workers/pool.ts`, `buildApp` from `../../src/server.ts`, `ConfigSchema` from `../../src/config/schema.ts`.

2. Helper functions (same pattern as upload.test.ts):
   - `sha256Hex(data: Uint8Array): Promise<string>` — compute SHA-256 hex
   - `makeDeleteAuth(hash: string): NostrEvent` — create kind 24242 event with `["t", "delete"]` tag, `["expiration", ...]` tag, and `["x", hash]` tag
   - `encodeAuth(event: NostrEvent): string` — encode as `Nostr ${base64url(JSON.stringify(event))}`

3. Setup test: create tmpDir, init DB, LocalStorage, pool(1 worker), two apps:
   - `appWithAuth`: `upload: { requireAuth: true, enabled: true }, delete: { requireAuth: true }`
   - Upload a test blob using a PUT /upload with auth so the blob is owned by the test pubkey.
   - Store `blobHash` and `cleanup` function in shared state.

4. Test cases:
   - **"DELETE blob: successful deletion returns 204 with empty body"** — send DELETE `/<blobHash>` with valid delete auth. Assert `res.status === 204`. Assert body is empty (await res.text() === "").
   - **"DELETE blob: non-existent hash returns 404"** — send DELETE `/${"f".repeat(64)}` with valid delete auth for that hash. Assert `res.status === 404`.
   - **"DELETE blob: invalid hash format returns 400"** — send DELETE `/not-a-valid-hash` with auth. Assert `res.status === 400`.
   - **"DELETE blob: missing auth when required returns 401"** — Upload a new blob first (so it exists). Send DELETE without Authorization header. Assert `res.status === 401`.
   - **"DELETE blob: non-owner returns 403"** — Upload a blob with sk1 (first key). Create a second secret key (sk2). Send DELETE with auth signed by sk2. Assert `res.status === 403`.

5. Teardown test: call `cleanup()`.

All tests use `{ sanitizeOps: false, sanitizeResources: false }`.
  </action>
  <verify>
    <automated>deno test --allow-net --allow-read --allow-write --allow-env --allow-ffi --allow-sys tests/e2e/delete.test.ts</automated>
  </verify>
  <acceptance_criteria>
- tests/e2e/delete.test.ts exists and contains at least 5 `Deno.test` blocks
- Test file contains `assertEquals(res.status, 204)` for successful deletion
- Test file contains `assertEquals(res.status, 404)` for non-existent blob
- Test file contains `assertEquals(res.status, 400)` for invalid hash
- Test file contains `assertEquals(res.status, 401)` for missing auth
- Test file contains `assertEquals(res.status, 403)` for non-owner
- `deno test tests/e2e/delete.test.ts` passes all tests
  </acceptance_criteria>
  <done>All DELETE endpoint status codes verified by passing E2E tests: 204 success, 404 not found, 400 invalid hash, 401 no auth, 403 non-owner</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> DELETE handler | Auth token and hash from untrusted client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | DELETE auth | accept | Existing BUD-11 auth middleware validates Nostr event signatures; no changes in this plan |
| T-02-02 | Tampering | DELETE hash param | accept | Existing SHA256_RE regex validates hash format; errorResponse(ctx, 400) on invalid |
| T-02-03 | Information Disclosure | DELETE 204 vs 404 | accept | Status code difference reveals blob existence, but this is inherent to the BUD spec and identical to GET behavior |
</threat_model>

<verification>
- `deno check src/routes/delete.ts` passes
- `deno test tests/e2e/delete.test.ts` — all tests pass
- `grep -c "ctx.body(null, 204)" src/routes/delete.ts` returns 2
- `grep -c "ctx.body(null, 200)" src/routes/delete.ts` returns 0
</verification>

<success_criteria>
- DELETE returns 204 for all successful deletions (both ownership-only removal and full purge)
- DELETE returns 404 for non-existent blobs
- All E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-read-side-operations/02-01-SUMMARY.md`
</output>
