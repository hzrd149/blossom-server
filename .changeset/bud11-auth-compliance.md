---
"blossom-server-ts": patch
---

Update auth token validation to comply with BUD-11: Base64url decoding, `created_at` in-past check, `server` tag domain scoping, and required `x` tag for upload/media HEAD requests
